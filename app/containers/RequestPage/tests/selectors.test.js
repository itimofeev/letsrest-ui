// import { fromJS } from 'immutable';
// import { makeSelectRequestPageDomain } from '../selectors';

// const selector = makeSelectRequestPageDomain();

describe('makeSelectRequestPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
